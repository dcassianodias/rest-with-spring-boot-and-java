spring:
  application:
    name: rest-with-spring-boot-and-java

  datasource:
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_and_java?useSSL=false&serverTimezone=UTC
    username: root
    password: 2012
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: true
    open-in-view: false


logging:
  level:
    # root: warn
    com.github.dcassianodias: debug